function initMap() {

    var map = new google.maps.Map(document.getElementById('map'), {
        center: { lat: 33.6405, lng: -117.8389 }, // New York City coordinates
        zoom: 12 // Zoom level
    });
}
